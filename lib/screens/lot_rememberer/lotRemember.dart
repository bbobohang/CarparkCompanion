import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class lotRemember extends StatelessWidget {
  const lotRemember({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lot Rememberer")),
    );
  }
}
